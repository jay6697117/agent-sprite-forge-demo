Create a top-down 2D pixel-art farmer character walk animation sprite sheet matching the provided reference character identity and palette. Same young farmer in all frames: messy brown hair, tan cream shirt, green pants, brown boots, small backpack strap details, warm RPG farming-game pixel style, compact chibi proportions, clean readable silhouette. No tools or props in hands; basic walking cycle only. Technical constraints: output is one square sprite sheet arranged as 4 columns by 4 rows; for generation each source grid cell is visually equal size with large solid magenta padding on all four sides; row order top to bottom is down, left, right, up; each row has exactly 4 frames forming neutral step neutral opposite-step walk loop; every character is centered inside its cell and must have at least 20 percent solid #FF00FF magenta margin above the head, below the feet, left of the body, and right of the body; the bottom row facing up must also be centered vertically and must NOT touch the top of its cells; character must remain same identity, same palette, same scale, same bounding-box size across all 16 cells; feet aligned consistently within each row; nothing crosses or touches cell edges; solid #FF00FF magenta background everywhere outside sprite pixels; pixel-art only, no anti-aliased painterly rendering, no blur; no labels, no UI, no text, no watermark, no frame numbers, no grid lines; output a single PNG sprite sheet.
